## 1. 建立回归基线

- [x] 1.1 添加失败的扫描器测试，覆盖空结果替换、准确的单文件端点计数、单次 include 枚举、注解预筛选和排除规则感知的 watcher 调度
- [x] 1.2 添加失败的激活/搜索测试，证明扫描结束前命令已注册，且索引为空时 QuickPick 仍可使用
- [x] 1.3 添加失败的缓存测试，将有界搜索与参考全量排序比较，覆盖同分稳定性和操作次数伸缩性
- [x] 1.4 添加失败的 Base URL 测试，证明重复查询跳过发现流程，且工作区级失效会强制重新发现

## 2. 修正并精简端点索引

- [x] 2.1 让 `scanFile` 返回端点数量，并始终替换文件缓存，包括预筛选和解析得到空结果的情况
- [x] 2.2 移除工作区扫描中的重复预删除和全局缓存大小推断，改为记录返回的单文件数量
- [x] 2.3 将配置的 include 模式合并为一次文件发现请求，同时保留标准化去重作为安全边界
- [x] 2.4 为 Java/Kotlin 源文件添加保守的受支持注解预筛选
- [x] 2.5 为 `FileWatcher` 添加文档 glob 语法的排除匹配，并随 watcher 配置重建过滤条件
- [x] 2.6 将常规逐文件扫描、watcher 和状态日志改为有界生命周期汇总，同时保留警告与错误

## 3. 提升激活与搜索响应性

- [x] 3.1 暴露扫描器运行状态和可释放的状态变更事件，不改变扫描串行化语义
- [x] 3.2 先注册服务、watcher、命令和订阅，再启动带异常捕获的后台首次扫描
- [x] 3.3 更新 SearchUI：展示索引状态、允许索引期间打开空选择器、输入时查询实时缓存，并保留扫描完成后的空缓存警告
- [x] 3.4 将已校验的 `maxResults` 同时应用于初始和查询后的 QuickPick 条目构造

## 4. 扩展端点搜索能力

- [x] 4.1 使用有界二叉堆替代有序数组 top-K 插入，并保留缓存位置以稳定同分顺序
- [x] 4.2 使用现有排名比较器排序堆中保留的候选，并验证与参考排序完全一致
- [x] 4.3 运行确定性大型缓存操作次数回归，并记录本地前后耗时样本作为非规范性证据

## 5. 通过事件使 Base URL 缓存失效

- [x] 5.1 扩展进程内解析器缓存，使其保存配置发现结果和解析值，缓存命中时不再遍历目录或执行 stat
- [x] 5.2 添加工作区级和全局失效 API，保持测试 reset 与边界克隆行为
- [x] 5.3 监听受支持 Spring 配置的创建、修改和删除事件，仅使所属工作区缓存失效
- [x] 5.4 扩展停用/reset 时释放配置 watcher 并清空进程内 Base URL 缓存

## 6. 验证并协调文档

- [x] 6.1 运行 `npm run compile`、`npm run lint`、`npm test`，以及参数、端点、URL/cURL、JSON-to-class 和 Marketplace 统计脚本
- [x] 6.2 运行 `npm run build` 并检查打包包含规则，确保测试、OpenSpec、source map 和声明文件继续被排除
- [x] 6.3 执行 neat-freak，同步 `AGENTS.md`、`CHANGELOG.md`、README、`docs/OPTIMIZATION_PLAN.md`、`docs/INCREMENTAL_SCAN.md` 和 `docs/DOCUMENTATION_MANIFEST.md`
- [x] 6.4 对照最终实现回读已保存的规格和文档，并确认全部 OpenSpec 任务已勾选

## 7. Code Review 并发安全加固

- [x] 7.1 配置热重载发生在扫描期间时，排队执行使用最新配置的强制全量扫描，并在该轮开始前清理旧索引
- [x] 7.2 区分文件扫描成功与失败，仅为成功读取和解析的文件记录扫描状态
- [x] 7.3 扩展停用时停止并等待后台扫描安全收束，同时清理扫描器全部定时器
- [x] 7.4 使用工作区版本令牌阻止失效期间启动的异步 Base URL 解析回填陈旧缓存

## 8. 二次 Code Review 收口

- [x] 8.1 停用时释放配置订阅并等待配置重载及任意当前扫描任务，阻止停用后重建 watcher
- [x] 8.2 当前扫描失败但已有强制刷新排队时继续执行刷新，并补充异常路径回归测试
- [x] 8.3 单一所有权管理状态栏隐藏定时器，防止前一轮扫描隐藏后一轮进度或遗留定时器
- [x] 8.4 删除 Base URL 缓存中未读取的配置文件列表字段，并完成全量验证与文档同步

## 9. 2026-08-12 Code Review 回归加固

- [x] 9.1 后台索引完成后刷新已打开 QuickPick，并覆盖空结果提示及状态订阅竞态
- [x] 9.2 解析前后校验文件 `mtime + size`，仅在元数据稳定时原子提交端点缓存和扫描状态
- [x] 9.3 为重复及 CRLF JAX-RS 注解块保留精确源码偏移和跳转行号
- [x] 9.4 watcher 同时匹配绝对路径与所属工作区相对排除模式
- [x] 9.5 运行完整测试、生产构建、专项脚本并使用 neat-freak 同步文档与规格
